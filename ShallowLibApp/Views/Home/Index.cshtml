
@using Microsoft.AspNetCore.Identity
@using AuthDatabase.Entities;
@model LibraryViewModel

@inject SignInManager<AppUser> SignInManager
@inject UserManager<AppUser> UserManager


@{
    ViewData["Title"] = "Home Page";
}


@if (SignInManager.IsSignedIn(User))
{

    @*@foreach (var library in Model.Items)
        {

            <div class="col-md-4">
                <a href="#">
                    <img src="@library.BlobID" alt="Image" style="max-width:100%;">
                </a>
                <div class="figure-caption">
                    <h3 class="pull-right">@library.Title</h3>
                </div>
            </div>

        }*@


    @foreach (var library in Model.Items)
    {

        <div class="col-sm-4 col-md-3 offset-md-1">

            <figure>
                <a href="#"><img src="@library.BlobID" ></a>

                <figcaption>@library.Title</figcaption>
                <a asp-controller="Home" asp-action="ItemByMediaType" asp-route-id="@library.TypeofMedia">@library.TypeofMedia</a>
            </figure>

        </div>


        @*<div class="col-lg-3 col-md-4 col-xs-6 thumb">
                <a class="thumbnail" href="" data-image-id="@library.ID" id="@library.BlobID" data-toggle="modal" data-title="@library.Title"
                   data-autor="@library.AutorName"
                   data-year="@library.Year"
                   data-image="@library.BlobID"
                   data-target="#image-gallery">
                    <img class="img-thumbnail zoom img-fluid" src="@library.BlobID" alt="">
                </a>


            </div>

            <div class="modal fade" id="image-gallery" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
                <div class="modal-dialog modal-lg">
                    <div class="modal-content">
                        <div class="modal-header col-md-12 ">

                            <div>
                                <h4 class="modal-title" id="image-gallery-title"></h4>
                                <div class="modal-title" id="image-gallery-autor"></div>
                                <button class=" btn btn-dark" > Update </button>
                            </div>




                            <button type="button" class="close" data-dismiss="modal">
                                <span aria-hidden="true">×</span><span class="sr-only">Close</span>
                            </button>
                        </div>
                        <div class="modal-body">


                            <img id="image-gallery-image" class="img-responsive col-md-6" src="@library.BlobID">
                            <a asp-controller="Home" asp-action="ItemByMediaType" asp-route-id="@library.TypeofMedia">@library.TypeofMedia</a>

                        </div>

                        <button type="button" class="btn btn-outline-dark ">
                            <i class="fa">Rent</i>
                        </button>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary float-left" id="show-previous-image">
                                <i class="fa fa-arrow-left"></i>
                            </button>

                            <button type="button" id="show-next-image" class="btn btn-secondary float-right">
                                <i class="fa fa-arrow-right"></i>
                            </button>
                        </div>
                    </div>
                </div>
            </div>*@

    }

    @*<div class="col-sm-4 col-lg-4 col-md-4">
            <div class="well well">

                <img class="thumbnail" src="@library.BlobID" alt="">
                <div class="figure-caption">
                    <h3 class="pull-right">@library.Title</h3>
                </div>
            </div>

        </div>*@



}


